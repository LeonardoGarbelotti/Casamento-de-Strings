#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* Funções:
                subpalavra(char palavra[], char fita[]);
                prefixo(char palavra[], char fita[]);
                sufixo(char palavra[], char fita[]);
    */
    /* E X E M P L O */
        subpalavra("amor", "mo");

        prefixo("odio", "od");

        sufixo("saudade", "ade");

    return 0;
}
